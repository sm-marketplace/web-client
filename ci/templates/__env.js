(function (window) {

  window.__env = {
    ENV: "${ENV}",
    CHAINS_ID: [/*'0x1',*/ '0x13881', '0x5', '0x539'],
    PROVIDER_URL: "${PROVIDER}",
    API: "${API}",
    IPFS_FILES_URL: "${IPFS_FILES_URL}",
  }
  
}(this));
